import { SignInFlow } from "@/types/auth-types";

interface SignInCardProps {
    setFormType : (state: SignInFlow) => void
}

export function SignInCard({setFormType : setState} : SignInCardProps){
    return (
        <div></div>
    );
}