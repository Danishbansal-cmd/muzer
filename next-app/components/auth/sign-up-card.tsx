import { SignInFlow } from "@/types/auth-types";

interface SignUpCardProps {
    setFormType : (state: SignInFlow) => void
}

export function SignUpCard({setFormType : setState} : SignUpCardProps){
    return (
        <div></div>
    );
}